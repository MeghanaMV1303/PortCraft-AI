export interface Project {
  id: string;
  title: string;
  techStack: string;
  description: string;
  link?: string;
  imageUrl?: string;
}

export interface Skill {
  id: string;
  name: string;
}
